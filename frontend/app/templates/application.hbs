<nav class="navbar navbar-default navbar-fixed-top" role="navigation">

    <div class="container-fluid">

        <div class="navbar-header">

            <button type="button" class="navbar-toggle" data-toggle="collapse"
                    data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>

            {{#if session.isAuthenticated}}

                {{#link-to "albums" class="navbar-brand" tabindex="-1"}}
                    <i class="fa fa-lg fa-picture-o"></i> Slidenight
                {{/link-to}}
            {{else}}


                {{#link-to "index" class="navbar-brand" tabindex="-1"}}
                    <i class="fa fa-lg fa-picture-o"></i> Slidenight
                {{/link-to}}
            {{/if}}

        </div>


        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">


            {{#if session.isAuthenticated}}

                <ul class="nav navbar-nav navbar-right">
                    <li>
                        <a title="Logout" {{action 'invalidateSession'}} href="#">
                            <span class="spacer"></span>
                            <i class="fa fa-sign-out fa-lg"></i>
                            </a>
                    </li>
                </ul>
            {{/if}}

            {{outlet 'menu'}}
        </div>

    </div>
</nav>

{{outlet}}
{{outlet 'modal'}}

<div class="navbar navbar-fixed-bottom app-footer">
    <div class="container-fluid">
        <p class="footer-item"> Slide shows come and go but slidenights are forever. <a target="_blank" href="http://twitter.com/xlfe">by xlfe</a>
        </p>
    </div>
</div>
